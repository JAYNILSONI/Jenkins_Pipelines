pipeline:
  agent:
    any: true
  
  environment:
    KEY1: "VAL1"
  
  stages:
    - name: 'Build'
      steps:
        - bat:
            script: 'echo "Building the project..."'
    
    - name: 'Test'
      steps:
        - bat:
            script: 'echo "Running tests..."'
    
    - name: 'Deploy'
      steps:
        - bat:
            script: 'echo "Deploying application..."'
