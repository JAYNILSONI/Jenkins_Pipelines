pipeline:
  agent: any
  environment:
    KEY1: "VAL1"
  stages:
    - name: "Build"
      steps:
        - bat: echo "Building the project..."
    - name: "Test"
      steps:
        - bat: echo "Running tests..."
    - name: "Deploy"
      steps:
        - bat: echo "Deploying application..."
